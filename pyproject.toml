[project]
name = "pomodoro-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyside6>=6.10.2",
    "winotify>=1.1.0",
]

[dependency-groups]
dev = [
    "nuitka>=4.0.5",
]
